# GoName

GoName is a tool that helps you renaming your video files.

## Features

- Rename a large amount of videos files
- Fetch information from TheMovieDB, TheTVDB or AniDB

## Usage

Run `goname plan --dir .`

Take a look at the results, if everything looks good, then you can `goname apply --dir .`
